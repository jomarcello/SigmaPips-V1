[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "poetry run uvicorn app.main:app --host 0.0.0.0 --port 9001"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure" 